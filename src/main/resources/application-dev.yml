spring:
  application:
    name: Cura

  datasource:
    url: jdbc:postgresql://localhost:5432/Cura
    username: postgres
    password: Tr3b3lforcity
    driver-class-name: org.postgresql.Driver

  jpa:
    database:
      platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate.ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        auto_index_ddl: true

api.prefix: /api/v1
